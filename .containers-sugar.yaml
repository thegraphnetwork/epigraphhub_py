version: 1.0.0
compose-app: docker-compose
service-groups:
  - name: default
    project-name: epigraphhubpy
    compose-path: docker/compose.yaml
    env-file: docker/.env
    service:
      default: postgres
      list:
        - name: postgres
